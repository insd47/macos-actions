# macOS App Icon

Reads `CFBundleIconFile` from the app's `Info.plist` and replaces the referenced `.icns` file. The action follows the
file name declared by the bundle instead of assuming the output conventions of Unity or any other build tool.
